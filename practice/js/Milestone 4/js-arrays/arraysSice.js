const names =['nayem', 'nahid', 'gittu', 'noyon', 'samim', 'saykat']; 
/* names[1]='utsho';
const slicer =names.slice(1,4);

console.log(slicer); */

// includes syntex
names.includes('nayem');

const kaka = 'this is fucking wesome';
kaka.includes('is');
console.log(kaka);