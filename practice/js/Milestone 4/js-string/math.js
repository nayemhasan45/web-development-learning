const power = Math.pow(2,3);
const dosomic = 4.444;
const fullNumber = Math.round(dosomic);
const seling = Math.ceil(dosomic);
const floor = Math.floor(dosomic);
console.log(floor);

// absulute value 
const first = 55;
const second = 65;
const gap = Math.abs(first-second);
if(gap<5){
    console.log('gap kom');
}else{
    console.log('gap besi');
}
// console.log(first,second);
// [ first, second] = [ second, first];
// console.log(num1,num2);