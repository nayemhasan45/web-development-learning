const line = 'tumi bondhu kala pakhi ami jano ki.sada sada kala kala.rong jomece sada kala';
const wordSplit = line.split(' ');
const sentence = line.split('.');
const alpha = line.split('');
console.log(line.indexOf('ki'));

// slice practice
const tumiSlice = line.slice(4,34);
console.log(tumiSlice);