// Hour to min conveter 
function hourtoMIn(hour){
    const convater = hour*60;
    return convater;
}
console.log(hourtoMIn(5),'min');

// min to second conveter

function mintoSeconds(min){
    const convater = min*60;
    return convater;
}
console.log(mintoSeconds(60),'second');