// write an arrow function to multyply 3 number 
const multyply = (x, y, z) => (x * y * z);
console.log(multyply(2, 3, 4));
// write the sentence in three line
const line = `I am a web developer. I love to code. I love to eat fuska`;
console.log(line);
// write a arrow function with one perameter default 
const sum = (x, y) => (x + y);
console.log(sum());